/**
 * LIVE: AppleScript → Supabase slide bridge.
 * Run on presenter Mac: node scripts/slide-bridge.js
 *
 * Requires: Keynote running, SUPABASE_URL, SUPABASE_SERVICE_KEY, EVENT_ID
 */

console.log(
  "Slide bridge stub — configure Supabase and Keynote AppleScript for live sync."
);
